---
apiVersion: v1
kind: Service
metadata:
  name: hotrod-route
  labels:
    appName: hotrod-route
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the route service.
  type: ClusterIP
  ports:
  - name: hotrod-route
    port: 8083
    protocol: TCP
    targetPort: hotrod-route
  selector:
    appName: hotrod-route
---
apiVersion: apps/v1beta2 #  for k8s versions before 1.9.0 use apps/v1beta2  and before 1.8.0 use extensions/v1beta1
kind: Deployment
metadata:
  name: GITLAB_CI_ENV_SLUG-hotrod-route
spec:
  selector:
    matchLabels:
      appName: hotrod-route
  replicas: REPLICA_COUNT
  template:
    metadata:
      labels:
        appName: hotrod-route
        track: APPLICATION_TRACK
        app: GITLAB_CI_ENV_SLUG
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
    spec:
      containers:
      - name: hotrod-route
        image: registry.hub.docker.com/willwangkelda/hotrod-route:COMMITID
        command: ["/go/bin/hotrod", "route"]
        env:
        - name: GET_HOSTS_FROM
          value: dns
        ports:
        - containerPort: 8083
